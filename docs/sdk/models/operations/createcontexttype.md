# CreateContextType

The type of owner. Defaults to "organization". Accounts are only used as context owners in server and must be specified by an id instead of a slug.

## Example Usage

```typescript
import { CreateContextType } from "circleci-v2-sdk/sdk/models/operations";

let value: CreateContextType = "organization";
```

## Values

```typescript
"organization"
```